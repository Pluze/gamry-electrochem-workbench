interface:
  display_name: "LabKit Test Planner"
  short_description: "Design effective LabKit validation evidence"
  default_prompt: "Use $labkit-test-planner to select and run the validation this task requires, reusing existing evidence."
